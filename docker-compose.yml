version: '3.9'

services:

  postgres:
    image: postgres:16

    container_name: postgres-auth

    environment:
      POSTGRES_DB: authdb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 123456

    ports:
      - "5432:5432"